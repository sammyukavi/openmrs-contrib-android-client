<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.openmrs.mobile">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.ACBaseActivity"
            android:label="@string/app_name"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.FindPatientRecord" />
        <activity
            android:name=".activities.ActiveVisits"
            android:noHistory="true" />
        <activity
            android:name=".activities.CaptureVitals"
            android:noHistory="true" />
        <activity
            android:name=".activities.RegisterPatient"
            android:noHistory="true" />
        <activity
            android:name=".activities.PatientLists"
            android:noHistory="true" />
        <activity
            android:name=".activities.VisitTasks"
            android:noHistory="true" />
        <activity
            android:name=".activities.Settings"
            android:noHistory="true" />
        <activity
            android:name=".activities.PatientDashboard"
            android:noHistory="true" />
    </application>

</manifest>